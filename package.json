{
  "name": "phonebook",
  "version": "0.0.1",
  "description": "Phonebook application backend",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../exercises/part2/phonebook/ && npm run build && cp -r dist ../../../phonebook-backend/",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yuri Hummel",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "mongoose": "^8.7.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
